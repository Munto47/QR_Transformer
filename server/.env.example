# 复制为 .env 后按需修改（.env 勿提交到 Git）
# 本地：PostgreSQL（也可用 Docker: postgres:16 + 5432）
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/qr_transformer?schema=public"
PORT=3100
# 云端一般可不设；默认 0.0.0.0 以便容器外访问
# HOST=0.0.0.0
# 生产构建托管前端静态资源
# NODE_ENV=production
