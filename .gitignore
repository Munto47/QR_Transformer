# 依赖与构建产物（应忽略）
node_modules/
dist/
build/

# 密钥与本地环境（勿提交真实 .env）
.env
.env.local
.env.*.local

# 本地数据库（可由迁移重新生成）
*.db
*.db-journal

# 系统文件
.DS_Store
Thumbs.db

# 根目录样例图若与 client/public/image.png 重复，可只提交 public 内一份
/image.png
